<Window x:Class="FolderOrganiser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderOrganiser"
        mc:Ignorable="d"
        Title="Folder Organiser" Height="450" Width="800"
        xmlns:vm="clr-namespace:FolderOrganiser.ViewModels">
    <Window.DataContext>
        <vm:CoreViewModel/>
    </Window.DataContext>
    <Grid>
        <Button Content="Choose Path" Click="Button_Click" Width="200" Height="75" Margin="10,10,590,349"/>
        <TextBlock Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" Height="50" Margin="215,23,10,361"/>
        <StackPanel Margin="215,88,0,0">
            <ItemsControl ItemsSource="{Binding Files}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,5" 
                         FontSize="16"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
