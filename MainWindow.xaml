<Window x:Class="FolderOrganiser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FolderOrganiser"
        Icon="/assets/logo.ico"
        mc:Ignorable="d"
        Title="Folder Organiser" Height="450" Width="800"
        xmlns:vm="clr-namespace:FolderOrganiser.ViewModels"
        xmlns:conv="clr-namespace:FolderOrganiser.Converters">
    <Window.DataContext>
        <vm:CoreViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <conv:FileSizeConverter x:Key="FileSizeConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.9*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.25*"/>
            <ColumnDefinition Width="0.75*"/>
        </Grid.ColumnDefinitions>
        
        <Button Content="Choose Path" Click="Button_Click" Grid.Row="0" Grid.Column="0"/>
        <TextBlock Text="{Binding Path, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" TextAlignment="Center" VerticalAlignment="Center"/>
        <ListView x:Name="SideBar" Grid.Column="0" Grid.Row="1" HorizontalContentAlignment="Stretch">
            <Button Content="Organise"></Button>
            <Button Content="Test"></Button>
            <Button Content="Settings" Click="Settings_Click"></Button>
        </ListView>
        
        <ListView  x:Name="ListStack" ItemsSource="{Binding Files}" Grid.Row="1" Grid.Column="1" Grid.RowSpan="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="250" />
                    <GridViewColumn Header="Extension" DisplayMemberBinding="{Binding Extension}" Width="80" />
                    <GridViewColumn Header="Size" DisplayMemberBinding="{Binding FileSize, Converter={StaticResource FileSizeConverter}}" Width="100" />
                    <GridViewColumn Header="Last Edited" DisplayMemberBinding="{Binding LastEdited, StringFormat={}{0:g}}" Width="150" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
